{
  "name": "gulp-html-subs",
  "version": "0.1.1",
  "description": "A plugin for Gulp",
  "keywords": [
    "gulpplugin"
  ],
  "author": {
    "name": "",
    "email": "",
    "url": "https://github.com/garysoed"
  },
  "repository": "garysoed/gulp-html-subs",
  "scripts": {
    "test": "istanbul test ./node_modules/mocha/bin/_mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "gulp-util": "^2.2.20",
    "jsdom": "^3.0.2",
    "through2": "^0.6.3"
  },
  "devDependencies": {
    "chai": "^1.10.0",
    "coveralls": "*",
    "event-stream": "*",
    "istanbul": "^0.3.5",
    "mocha": "^2.1.0",
    "mocha-lcov-reporter": "*",
    "should": "~2.1.0"
  },
  "engines": {
    "node": ">=0.8.0",
    "npm": ">=1.2.10"
  },
  "license": "MIT"
}
